============ README =================================
to build: make

to build bonus: make bonus

with the bonus build, you can use this following
command to create the AST in .png format and open it
	dot -Tpng /tmp/tree.dot -o tree.png && open tree.png
or simply
	make drawast
=====================================================
